import LaunchList from '../launches/LaunchList'

// To do: Perhaps a container page to hold different launch components.
function Launches() {
  return (
    <div>
      <LaunchList />
    </div>
  )
}

export default Launches
